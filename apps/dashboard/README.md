# apps/dashboard

Frontend del Worktrees Manager: Vite + React + TypeScript (SPA, sin SSR). Ver `docs/ARCHITECTURE.md` §2 para las convenciones de esta app y `docs/ROADMAP.md` para el estado actual del proyecto.

```
pnpm --filter dashboard dev
```
